<% if @user.errors.any? %>
  <div id="errors">
    <ul>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= form_for build_user(@user) do |f| %>
  <% if params[:all].nil? %>
    <%= render 'users/steps/current_step', f: f %>
  <% else %>
    <%= render 'users/steps/all_steps', f: f %>
  <% end %>
<% end %>
